<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout android:layout_height="match_parent"
    android:layout_width="match_parent"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <EditText
        android:id="@+id/fragment_my_thanks_first_edit_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="35dp"
        app:layout_constraintTop_toTopOf="parent"
        android:maxLines="5"
        android:paddingRight="50dp"
        />

    <Button
        android:id="@+id/daily_thanks_next_btn"
        android:layout_width="50dp"
        android:layout_height="30dp"
        app:layout_constraintRight_toRightOf="@id/fragment_my_thanks_first_edit_text"
        app:layout_constraintTop_toTopOf="@id/fragment_my_thanks_first_edit_text"
        app:layout_constraintBottom_toBottomOf="@id/fragment_my_thanks_first_edit_text"
        android:text="다음"
        android:textSize="10sp"
        android:textColor="@color/whiteColor"
        android:background="@null"
        android:visibility="visible"/>
</androidx.constraintlayout.widget.ConstraintLayout>